const Mysql = require('./mysql');
const util = require('util');

class Execute extends Mysql{
  constructor(pool,options) {
    super(pool,options);
  }
  async _fn(d,cb) {
    const d = await this.query(d,cb);
    if (this.options.pushResult) return d;
  }
}

module.exports = Execute;